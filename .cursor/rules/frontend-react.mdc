---
description: Admin React 应用约束
globs:
  - "apps/admin/src/**/*.ts"
  - "apps/admin/src/**/*.tsx"
alwaysApply: true
---

- 路由鉴权统一在 `app/guards` 实现，页面组件不自行判断权限跳转。
- 表单必须使用 `react-hook-form + zod`，schema 放在 `features/*/schemas`。
- 服务端状态使用 React Query，本地 UI 状态使用 Zustand，避免混用。
- 页面层通过 hooks 访问数据（`features/*/api`），禁止直接调用 axios。
- 路由文件中声明角色元信息，涉及访问控制的页面必须走 `AuthGuard`。
- 环境变量必须在 `vite-env.d.ts` 中补类型声明，避免 `import.meta.env` 隐式 any。
- 可复用 UI 优先放在 `packages/ui`，页面仅做业务组合。
