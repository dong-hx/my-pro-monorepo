{
  "$schema": "https://turbo.build/schema.json",
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "env": ["NODE_ENV", "APP_MODE"],
      "outputs": ["dist/**", ".next/**"]
    },
    "preview": {
      "dependsOn": ["build"],
      "env": ["NODE_ENV", "APP_MODE"],
      "cache": false,
      "persistent": true
    },
    "dev": {
      "cache": false,
      "persistent": true
    },
    "lint": {
      "dependsOn": ["^lint"],
      "inputs": [
        "src/**/*.{ts,tsx,js,jsx}",
        "test/**/*.{ts,tsx}",
        "eslint.config.js",
        "eslint.config.mjs",
        "../../eslint.config.mjs",
        "package.json"
      ]
    },
    "test": {
      "dependsOn": ["build"],
      "outputs": ["coverage/**"]
    },
    "clean": {
      "cache": false,
      "outputs": []
    }
  }
}
